name: git command test

on:
  schedule:
    - cron: '15 12 * * 5'

jobs:
  chk-branch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: git status
      - run: git branch
